# Rspamd action thresholds.
# discard is raised to 999 so no inbound mail is ever silently dropped —
# mail above add_header (6) goes to per-user Junk via sieve-before instead.
# reject remains at 15 for clear-cut spam/viruses.
greylist = 4;
add_header = 6;
reject = 15;
discard = 999;
